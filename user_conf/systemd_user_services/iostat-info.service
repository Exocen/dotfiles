[Unit]
Description=Iostat info
PartOf=graphical-session.target
StopWhenUnneeded=yes

[Service]
Type=simple
Restart=always
ExecStart=%h/.config/sway/iostat-info

[Install]
WantedBy=sway-session.target
