# Profile
# cat cloud-profile1.profile | sudo lxc profile edit cloud-profile

# Launch sample
# sudo lxc launch images:debian/11/cloud testuser -p cloud-profile

# connect sample
# sudo lxc exec testuser -- su --login exo
# sudo lxc exec testuser -- /bin/bash

#cloud-init (must be installed outside container)
#run in container:
#cloud-init status --wait
