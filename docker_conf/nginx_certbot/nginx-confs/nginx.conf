events {}
http {
    include mime.types;
    charset utf-8;
    server_tokens off;
    client_max_body_size 20m;
    server {
        listen 80 default_server;
        listen [::]:80 default_server;
        server_name [DOMAIN] *.[DOMAIN];

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        server_name = [DOMAIN];
        listen 443 quic reuseport;
        listen [::]:443 quic reuseport;
        include http3-spec.conf;
        log_not_found off;

        location / {
            root /usr/share/nginx/main;
        }

        location /filebrowser {
            alias /usr/share/nginx_local;
            index  _;
            autoindex on;
            autoindex_exact_size off;
            autoindex_format html;
            auth_basic "Restricted Access";
            auth_basic_user_file /etc/nginx/htpasswd;
        }

    }

    server {
        server_name = status.[DOMAIN];
        include http3.conf;

        location / {
            root /usr/share/nginx/status;
        }
    }

    server {
        server_name = vw.[DOMAIN];
        include http3.conf;

        location / {
            proxy_pass http://10.0.0.80:80/;
            include proxy-pass.conf;
        }
    }

    server {
        server_name = git.[DOMAIN];
        include http3.conf;

        location / {
            proxy_pass http://10.0.0.81:3000/;
            include proxy-pass.conf;
        }
    }

    server {
        server_name = fmd.[DOMAIN];
        include http3.conf;

        location / {
            proxy_pass http://10.0.0.83:8080/;
            include proxy-pass.conf;
        }
    }

    server {
        server_name = mail.[DOMAIN];
        include http3.conf;

        location / {
            proxy_pass http://10.0.0.82:8888/;
            include proxy-pass.conf;
        }
    }
}
